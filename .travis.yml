cache: packages
sudo: required
warnings_are_errors: false
services:
  - docker
before_install:
-  docker-compose  run retj
script:
# -  docker-compose run retj julia test/runtests.jl || exit 1
-  docker-compose run --service-ports retj julia test/runtests.jl || exit 1
# - docker-compose run --service-ports retj bash
# cp cli_tools/.pgpass  ~/
# psql -U postgres -h pgdb  -d testdb -w
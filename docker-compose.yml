version: '3'

services:
  mysqldb:
    image: mysql:5.7
    environment:
      MYSQL_USER: travis
      MYSQL_PASSWORD: Password12!
      MYSQL_ROOT_PASSWORD: Password12!
      MYSQL_DATABASE: testdb
    # ports:
    #   - "3309:3306"

  pgdb:
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Password12!
      POSTGRES_DB: testdb

  # admin:
  #   image: adminer
  #   ports:
  #     - 8080:8080

  retre:
    container_name: rertne
    image: rertie
    command: bash -c "python --version"
    build: . 
    depends_on:
      - mysqldb
      - pgdb
    ports:
      - 5001:80
 